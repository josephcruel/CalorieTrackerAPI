<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Calorie Tracker App</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Calorie Journal</h1>
        <a href="/calories/new" class="btn btn-success">Create New Entry</a>

        <% calories.forEach(calorie => { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%= calorie.name %></h4>
                 <div class="card-subtitle text-muted mb-2">
                    <%= calorie.createdAt.toLocaleDateString() %>
                 </div>
                 <div class="card-text mb-2">Calories: <%= calorie.calorie %></div>
                 <div class="card-text mb-2">Sodium: <%= calorie.sodium %></div>
                 <div class="card-text mb-2">Protein: <%= calorie.protein %></div>
                 <a href="calories/<%= calorie.slug %>" class="btn btn-primary">Read More</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>